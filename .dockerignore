.git
.github
.env
.local
dist
zot
zot-dev
internal/zotui/worker/artifacts/*.gz
# A go.work someone created locally must not reach the build: its go line would
# cap the toolchain below what go.mod requires, and the build would fail with a
# version error rather than anything that names the cause.
go.work
go.work.sum
